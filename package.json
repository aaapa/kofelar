{
  "name": "kofelar",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "vite": "^8.0.14",
    "vite-plugin-html-inject": "^1.1.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.100.0"
  }
}
